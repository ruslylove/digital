\documentclass[border=10pt]{standalone}
\usepackage{circuitikz}
\usetikzlibrary{shapes, arrows.meta, positioning, calc}

\begin{document}
\begin{circuitikz}[
    block/.style={draw, rectangle, minimum height=3cm, minimum width=4cm, align=center, fill=blue!10},
    memblock/.style={draw, rectangle, minimum height=3cm, minimum width=2.5cm, align=center, fill=green!10},
    outblock/.style={draw, rectangle, minimum height=3cm, minimum width=2.5cm, align=center, fill=orange!10},
    >=Latex
]

    % Combinational Logic Block (Left)
    \node[block] (comb) at (0,0) {\textbf{Combinational}\\ \textbf{Logic}};

    % Memory Elements Block (Right of Comb)
    \node[memblock] (mem) at (6,0) {\textbf{Memory}\\ \textbf{Logic}};

    % Output Logic Block (Shifted further Right of Mem)
    \node[outblock] (out) at (12,0) {\textbf{Output}\\ \textbf{Logic}};

    % Inputs -> Comb
    \draw[<-] (comb.west) -- ++(-2,0) node[anchor=east] {Inputs};

    % Comb -> Mem (Next State)
    \draw[->, thick] (comb.east) -- (mem.west) node[midway, above, font=\small] {Next State};

    % Mem -> Out (Present State)
    \draw[->, thick] (mem.east) -- (out.west) node[midway, above, font=\small] {Present State};

    % Feedback Loop: Mem East -> Comb South (Solid line, to area below inputs)
    % Start from Mem East (split point)
    \coordinate (feedbackStart) at ($(mem.east)!0.5!(out.west)$);
    
    % Draw solid line with arrow
    \draw[->, thick] (feedbackStart) -- ++(0, -3) -- ($(comb.west) + (-1, -3)$) |- ($(comb.west) + (0, -0.5)$);
    
    % Use coordinate calculation to land on the South border? 
    % User requested: "point back to com. at south location below the inputs"
    % Inputs are at West. "Below the inputs" usually implies the South side near the West edge.
    % So we route to comb.south roughly at x coordinate near west.
    
    % Let's redraw path clearly:
    % From split, go down. Go left until under Comb. Go up to Comb South.
    %\draw[->] (feedbackStart) -- ++(0, -3.0) -- ($(comb.south) - (1.0, 3.0)$) -- ($(comb.south) - (1.0, 0)$);

    \node[font=\small] at (3, -2.7) {Feedback (Present State)};

    % Comb -> Out (Direct Input/Logic Path for Mealy)
    \draw[->, dashed] ($(comb.west)+(-1,0)$) -- ++(0, 2) -| (out.north);
    %\node[font=\small, above] at (6, 1) {Input / Logic Path};

    % Clock to Memory - Correct Triangle Symbol
    % Draw triangle inside bottom of mem block
    \draw ($(mem.south) + (-0.2, 0)$) -- ($(mem.south) + (0, 0.3)$) -- ($(mem.south) + (0.2, 0)$);
    \draw (mem.south) -- ++(0,-0.4) node[below] {Clock};

    % Outputs
    \draw[->] (out.east) -- ++(1.5,0) node[anchor=west] {Outputs};

\end{circuitikz}
\end{document}
